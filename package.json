{
  "name": "jan-samadhan-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm ci && npm run build",
    "start": "cd backend && npm start"
  },
  "workspaces": [
    "backend"
  ]
}